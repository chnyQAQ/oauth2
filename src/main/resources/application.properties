security.oauth2.client.client-id=chny-client
security.oauth2.client.client-secret=chny623020

# SECURITY OAUTH2 CLIENT (OAuth2ClientProperties)
##security.oauth2.client.client-id= # OAuth2 client id.
##security.oauth2.client.client-secret= # OAuth2 client secret. A random secret is generated by default

# SECURITY OAUTH2 RESOURCES (ResourceServerProperties)
##security.oauth2.resource.id= # Identifier of the resource.
##security.oauth2.resource.jwt.key-uri= # The URI of the JWT token. Can be set if the value is not available and the key is public.
##security.oauth2.resource.jwt.key-value= # The verification key of the JWT token. Can either be a symmetric secret or PEM-encoded RSA public key.
security.oauth2.resource.jwk.key-set-uri=127.0.0.1:8080/.well-known/jwks.json
##security.oauth2.resource.prefer-token-info=true # Use the token info, can be set to false to use the user info.
##security.oauth2.resource.service-id=resource #
##security.oauth2.resource.token-info-uri= # URI of the token decoding endpoint.
##security.oauth2.resource.token-type= # The token type to send when using the userInfoUri.
##security.oauth2.resource.user-info-uri= # URI of the user endpoint.

# SECURITY OAUTH2 SSO (OAuth2SsoProperties)
##security.oauth2.sso.login-path=/login # Path to the login page, i.e. the one that triggers the redirect to the OAuth2 Authorization Server


server.port=8080

logging.level.org.springframework.security=DEBUG

spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://192.167.6.16:3306/panda-gateway?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=ciotea@db

spring.redis.host=127.0.0.1
spring.redis.database=0